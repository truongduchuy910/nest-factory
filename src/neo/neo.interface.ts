/**
 * 📌 NEO INTERFACE
 */

export interface NeoInterface {
  /* identity */
  id?: string;
  /* label */
  label?: string;
  /* created at */
  createdAt?: Date;
  /* created by */
  createdBy?: string;

  string?: string;
  number?: number;
  date?: Date;
}
